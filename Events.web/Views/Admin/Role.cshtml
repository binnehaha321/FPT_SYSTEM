@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Role";
}

    <h2>ROLE LIST</h2>

    @Html.ActionLink("Create Role", "RoleCreate", "Admin")

<div>
    <table class="table">
        <tr style="background-color: darkorange">
            <th>
                Role Name
            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Name
                </td>
            </tr>
        }
    </table>
</div>